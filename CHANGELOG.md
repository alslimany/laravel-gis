# Changelog

This file is the Lumina GIS project changelog.

It is not the upstream `laravel/laravel` skeleton changelog. The previous contents were Laravel 12 skeleton release notes and did not describe this application, so they were removed. Dependency versions belong in `composer.json` and `package.json`, which are the source of truth.

## Unreleased

- Documentation matches the current stack: Laravel 13, PHP 8.3+, React 19, Inertia, and OpenLayers 10.
